您是一个专注于本项目的 AI 助手，熟悉的项目相关配置和命名约定。

国际化配置：
- 国际化配置在 `productx-backend-react/productx/src/locales` 文件中。
- 请确保在添加各国新的国际化配置时，遵循上述规则并更新 `productx-backend-react/productx/src/locales/` 文件夹。
- 国际化配置使用 `react-i18next` 插件。
- 国际化配置使用 `/productx-backend-react/productx/src/i18n.js` 文件。
- ar的配置在 `/productx/src/locales/ar/translation.json` 文件中。
- de的配置在 `/productx/src/locales/de/translation.json` 文件中。
- en的配置在 `/productx/src/locales/en/translation.json` 文件中。
- es的配置在 `/productx/src/locales/es/translation.json` 文件中。
- fr的配置在 `/productx/src/locales/fr/translation.json` 文件中。
- it的配置在 `/productx/src/locales/it/translation.json` 文件中。
- ja的配置在 `/productx/src/locales/ja/translation.json` 文件中。
- ko的配置在 `/productx/src/locales/ko/translation.json` 文件中。
- ru的配置在 `/productx/src/locales/ru/translation.json` 文件中。
- zh的配置在 `/productx/src/locales/zh/translation.json` 文件中。


路由配置：
- 路由配置在 `productx-backend-react/productx/src/routes.js` 文件中。
- 请确保在添加新路由时，遵循上述规则并更新 `productx-backend-react/productx/src/routes.js` 文件。

依赖配置：
- 依赖配置在 `productx/package.json` 文件中。
- 请确保在添加新依赖时，遵循上述规则并更新 `productx/package.json` 文件。

分页参数只支持：
- pageSize：分页大小
- currentPage：当前页

API请求：
- 接口拦截器在 `productx/src/axiosInstance.js` 文件中。
- 非常重要：必须注意productx/src/axiosInstance.js已经做了一些处理，不要再下层多次处理data

添加翻译配置时：
- 请确保在添加新翻译时，请直接在配置末尾添加，我自己校对重复的情况。

上传图片：
- manage模组下，后台管理上传图片使用MultipartFile类，上传图片接口为/manage/image/upload上传图片成功后，返回图片url。然后创建和修改接口就可以使用此url填充图片字段。

管理页面生成流程：
- 管理页面统一放在 `productx/src/views/base` 路径下。
- 每个管理页面需要创建一个独立的文件夹，文件夹名称使用小驼峰命名法（例如：userList、adminRole）。
- 每个管理页面文件夹下必须包含以下4-5个核心组件文件：

1. **主页面组件**（例如：`Manager.js`）
   - 负责整体页面布局和状态管理
   - 包含搜索表单（使用 Ant Design 的 Row、Col、Input、Select 等组件）
   - 实现分页逻辑（使用 `src/components/common/Pagination` 组件）
   - 管理所有模态框的显示状态
   - 实现数据获取（使用 `src/axiosInstance` 进行 API 调用）
   - 实现批量选择和批量删除功能（使用 `UseSelectableRows` 和 `HandleBatchDelete`）
   - 接口路径规范：`/manage/{模块名}/list`（分页列表）
   - 分页参数必须使用：`currentPage`（当前页）和 `size`（分页大小）
   - 状态更改接口：`/manage/{模块名}/change-status`
   - 创建接口：`/manage/{模块名}/create`
   - 更新接口：`/manage/{模块名}/update`
   - 删除接口：`/manage/{模块名}/remove`
   - 批量删除接口：`/manage/{模块名}/delete-batch`

2. **表格组件**（例如：`ManagerTable.js`）
   - 纯展示组件，接收 props 传入的数据和事件处理函数
   - 使用原生 `<table>` 标签（不使用 Ant Design Table）
   - 必须包含复选框列，支持全选和单选
   - 包含操作列，提供"详情"、"修改"、"删除"按钮
   - 状态字段使用开关组件展示（toggle-switch）
   - 支持头像展示（使用 Ant Design Avatar 组件）
   - Props 包括：data、selectAll、selectedRows、handleSelectAll、handleSelectRow、handleStatusChange、handleEditClick、handleDeleteClick、handleDetailClick

3. **创建表单模态框**（例如：`ManagerCreateFormModal.js`）
   - 使用 Ant Design Modal 和 Form 组件
   - Form 布局使用 `layout="vertical"`
   - 支持双列布局（使用 flex 或 Row/Col）
   - 表单验证规则使用 Form.Item 的 rules 属性
   - 图标使用 Ant Design Icons（如 UserOutlined、MailOutlined 等）
   - 提交时格式化数据并调用父组件传入的 onFinish 方法
   - Props 包括：isVisible、onCancel、onFinish、form

4. **更新表单模态框**（例如：`UpdateManagerModal.js`）
   - 结构类似创建表单模态框
   - 在显示时自动获取详细数据并填充表单（使用 `/manage/{模块名}/get-by-id` 接口）
   - 使用 useEffect 监听 isVisible 和选中项的变化
   - 某些只读字段需要设置 disabled 属性
   - Props 包括：isVisible、onCancel、form、handleUpdateManager、selectedManager

5. **详情模态框**（例如：`ManagerDetailModal.js`）
   - 使用 Ant Design Modal 和 Descriptions 组件
   - 使用 `bordered` 和 `column={1}` 属性
   - 支持加载状态（使用 Spin 组件）
   - 详情接口同样使用：`/manage/{模块名}/get-by-id`
   - 使用 Badge 展示状态信息
   - 使用 Tag 展示标签类信息（如角色列表）
   - 可以使用 styled-components 自定义样式
   - Props 包括：isVisible、onCancel、{模块名}Id

**代码规范：**
- 所有 API 请求必须使用 `src/axiosInstance`，不要直接处理 response.data，因为拦截器已经处理
- 状态管理使用 React Hooks（useState、useEffect）
- 表单管理使用 Ant Design Form.useForm()
- 错误处理使用 try-catch 并使用 message 组件提示
- 加载状态使用 isLoading 和 Spin 组件
- 所有模态框使用 open 属性（而非 visible，Ant Design 5.x）

**数据结构约定：**
- 用户提供分页返回的数据结构后，根据字段生成对应的：
  1. 搜索表单字段
  2. 表格列
  3. 创建表单字段
  4. 更新表单字段
  5. 详情展示字段
- 通用字段映射：
  - id: 主键
  - status: 状态（布尔值，true=启用，false=禁用）
  - createTime/updateTime: 时间字段
  - createBy/updateBy: 创建人/更新人

**命名规范：**
- 文件名使用大驼峰命名法（PascalCase）
- 组件名与文件名保持一致
- 接口函数使用小驼峰命名法（camelCase）
- 常量使用大写下划线命名法（UPPER_SNAKE_CASE）

**路由配置：**
- 每次生成新的管理页面后，必须在 `productx/src/routes.js` 文件中添加对应的路由配置
- 路由配置包含两部分：

1. **导入组件**（在文件顶部添加）：
   ```javascript
   const {模块名}Router = React.lazy(() => import('./views/base/{文件夹名}/{主组件名}'));
   ```
   例如：
   ```javascript
   const ListManager = React.lazy(() => import('./views/base/manager/Manager'));
   ```

2. **添加路由规则**（在 routes 数组中添加）：
   ```javascript
   {
     path: '/data/{路由路径}',
     name: '{页面中文名称}',
     element: (
       <PrivateRoute>
         <{模块名}Router />
       </PrivateRoute>
     ),
   },
   ```
   例如：
   ```javascript
   {
     path: '/data/manager',
     name: '员工管理',
     element: (
       <PrivateRoute>
         <ListManager />
       </PrivateRoute>
     ),
   },
   ```

- 路由路径规范：
  - 系统管理类：`/data/{模块名}`
  - 业务模块类：`/sys/{模块名}`
  - 基础功能类：`/base/{模块名}`
- 所有业务页面必须使用 `<PrivateRoute>` 包裹以实现权限控制














